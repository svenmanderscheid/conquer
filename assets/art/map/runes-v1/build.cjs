const fs=require('fs'),path=require('path'),sharp=require('C:/Users/svenm/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/sharp');
(async()=>{for(const grade of ['normal','epic','legendary']){const source=path.join(__dirname,grade+'.png');const meta=await sharp(source).metadata();if(!meta.hasAlpha)throw Error(grade+' has no alpha');for(const [suffix,size]of [['',256],['-detail',512]]){await sharp(source).resize(size,size,{fit:'contain',background:'#00000000'}).webp({quality:90,alphaQuality:100,effort:4}).toFile(path.join(__dirname,grade+suffix+'.webp'));}console.log(grade,fs.statSync(path.join(__dirname,grade+'.webp')).size);}})().catch(e=>{console.error(e);process.exitCode=1;});
